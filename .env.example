# Public template only (safe to publish).
# For private runtime config, prefer: ~/.openclaw/private/home-assistant.env

# REQUIRED
HA_TOKEN=YOUR_LONG_LIVED_ACCESS_TOKEN

# Recommended for most users (Home Assistant Cloud / remote access)
HA_URL_PUBLIC=https://your-home.example.com

# Optional local URL (used first when HA_URL override is not set)
# If local is unreachable, scripts fall back to HA_URL_PUBLIC.
# HA_URL_LOCAL=http://homeassistant.local:8123

# Optional explicit override (highest priority, disables local/public fallback)
# HA_URL=http://homeassistant.local:8123
